#!/usr/bin/env bash
# mitmflow-body — Render request or response bodies via the shared renderer
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/mitmflow-render.py" body "$@"
