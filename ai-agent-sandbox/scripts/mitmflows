#!/usr/bin/env bash
# mitmflows — List captured mitmweb flows via the shared renderer
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/mitmflow-render.py" list "$@"
